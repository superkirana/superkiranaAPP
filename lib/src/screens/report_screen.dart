
// Report Screen
import 'package:flutter/material.dart';

class ReportScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Report"),
        backgroundColor: Colors.orange,
      ),
      body: Center(
        child: Text("Report Screen Content (Charts, Date Range, etc.)"),
      ),
    );
  }
}

